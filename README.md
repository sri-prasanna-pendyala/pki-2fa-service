# PKI 2FA Service 

This repository contains the implementation of a secure PKI-based Two-Factor Authentication microservice.
  
It includes RSA 4096-bit key generation, seed decryption using RSA-OAEP, TOTP generation, verification, Docker containerization, and cron-based 2FA code logging.